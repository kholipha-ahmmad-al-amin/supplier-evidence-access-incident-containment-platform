import express from'express';import{join}from'node:path';import{JsonIncidentStore}from'./store.mjs';import{IncidentService}from'./domain.mjs';const store=new JsonIncidentStore(process.env.DATA_FILE||join(process.cwd(),'data','incidents.json')),service=new IncidentService(store.load(),state=>store.save(state)),app=express(),safe=fn=>(req,res)=>{try{fn(req,res)}catch(error){res.status(400).json({error:error.message})}};app.use(express.json());app.get('/health',(_,res)=>res.json({status:'ok',service:'supplier-evidence-access-incident-containment-platform'}));app.get('/incidents',(_,res)=>res.json(service.state.incidents));app.post('/incidents',safe((req,res)=>res.status(201).json(service.report(req.body))));app.post('/incidents/:id/contain',safe((req,res)=>res.json(service.contain(req.params.id,req.body.actor,req.body.action))));app.post('/incidents/:id/authorize',safe((req,res)=>res.json(service.authorize(req.params.id,req.body.actor))));app.post('/incidents/:id/recover',safe((req,res)=>res.json(service.recover(req.params.id,req.body.actor))));app.post('/incidents/:id/attest',safe((req,res)=>res.json(service.attest(req.params.id,req.body.actor))));app.post('/incidents/:id/validate',safe((req,res)=>res.json(service.validate(req.params.id,req.body.actor))));app.get('/incidents/:id/history',safe((req,res)=>res.json(service.history(req.params.id))));app.listen(process.env.PORT||59900,'0.0.0.0');
